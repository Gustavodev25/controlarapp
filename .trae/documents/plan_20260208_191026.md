I will optimize the "Visão Geral" (Overview) section of the dashboard to improve performance without removing any animations.

**Technical Approach:**

1.  **Extract `DelayedLoopLottie` Component**:
    *   Move the local `DelayedLoopLottie` component from `dashboard.tsx` to a new reusable file `components/ui/DelayedLoopLottie.tsx`. This avoids code duplication and allows reuse.

2.  **Create `OverviewSection` Component**:
    *   Create a new component `components/dashboard/OverviewSection.tsx`.
    *   Move the "Visão Geral" UI logic (lines 1017-1209) into this component.
    *   **Optimization**: Use `useMemo` to cache the heavy financial calculations (Income, Expenses, Balance, Projections). Currently, these run on *every* render (e.g., when scrolling or opening menus), causing lag. With `useMemo`, they will only recalculate when the underlying data actually changes.
    *   **Optimization**: Wrap the component in `React.memo` to prevent it from re-rendering when unrelated parts of the dashboard update (like the chart animation or menu toggle).

3.  **Update `DashboardScreen`**:
    *   Replace the inline "Visão Geral" code with the new `<OverviewSection />` component.
    *   Pass the necessary data (`transactions`, `balances`, `settings`) as props.

**Benefits:**
*   **Faster Rendering**: Calculations happen only when necessary, not on every frame.
*   **Smoother Animations**: Isolating the component prevents it from slowing down other animations in the dashboard.
*   **Cleaner Code**: Reduces the complexity of the main `DashboardScreen` file.

**Files to be modified/created:**
*   `components/ui/DelayedLoopLottie.tsx` (New)
*   `components/dashboard/OverviewSection.tsx` (New)
*   `app/(tabs)/dashboard.tsx` (Refactor)
