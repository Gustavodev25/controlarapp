I will perform a complete revision of the number layout system to ensure uniform and proportional spacing as requested.

### Plan

1.  **Refactor `AnimatedCurrency.tsx`**:
    *   Remove the "magic" spacing calculations (`marginHorizontal` and `letterSpacing` based on font size) that were causing inconsistent results.
    *   Implement a standardized spacing system where `letterSpacing` is explicitly controlled via props, ensuring predictability.
    *   Set a sensible default `letterSpacing` (e.g., `-0.5`) to ensure numbers look natural by default.

2.  **Adjust `OverviewSection.tsx` ("Number Flow" / Saldo Estimado)**:
    *   The user reported these are "too close".
    *   I will adjust the `letterSpacing` to be more open (relaxing it from the previous effectively tight value).
    *   Ensure the "R$" prefix and the number are aligned correctly.

3.  **Adjust `dashboard.tsx` ("Minhas Contas" / Cards)**:
    *   The user reported these are "excessively separated".
    *   I will apply a tighter `letterSpacing` specifically for the card view to bring the digits together.
    *   Verify the alignment of the "R$" prefix and the number within the card layout.

4.  **Verification**:
    *   I will ensure the styles are flattened correctly so they are passed to the underlying `NumberFlow` component without issues.
    *   I will double-check that the changes apply to different resolutions by using relative units or consistent font scaling.

This approach addresses the specific complaints: loosening the tight "Saldo Estimado" and tightening the loose "Card Numbers", while establishing a consistent codebase pattern.
