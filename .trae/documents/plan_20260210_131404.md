I will implement the credit card invoice notification system by extending the existing `notificationService`.

### 1. Update `services/notifications.ts`
- **Add `InvoiceNotificationPreferences` interface**: To handle customizable notification days (default: 3 days).
- **Add `scheduleInvoiceNotifications` method**:
  - It will accept `CreditCardAccount[]` and `userId`.
  - It will iterate through accounts to find the next due date using `currentBill.dueDate` or `balanceDueDate`.
  - It will schedule notifications for:
    - **Reminder**: N days before due date (default 3).
    - **Due Date**: On the day of maturity.
    - **Overdue**: 1 day after if not paid (optional/future proofing).
  - It will optionally include the total amount in the message if available (`currentBill.totalAmount`).
  - It will use `AsyncStorage` to track sent notifications and avoid duplicates (idempotency), similar to the existing `scheduleRecurrenceNotifications`.
  - It will log activities using the existing `logActivity` helper.
- **Add `logInvoiceToExternalService`**: To log invoice notifications to Firebase (mirroring existing logic).

### 2. Update `app/(tabs)/invoices.tsx`
- Call `notificationService.scheduleInvoiceNotifications` inside the `fetchData` effect, ensuring notifications are refreshed whenever invoice data is loaded.

### 3. Update `__tests__/notificationService.test.ts`
- Add unit tests for `scheduleInvoiceNotifications`:
  - Verify notifications are scheduled for accounts with upcoming due dates.
  - Verify "Amount" is included in the message when available.
  - Verify no notification is scheduled if the due date is far in the future (outside the trigger window) or already passed.

### 4. Verification
- I will run the tests using `npm test` (or the appropriate test runner command for this project) to ensure the logic is correct.
