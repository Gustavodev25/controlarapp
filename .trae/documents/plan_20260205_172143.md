I will implement the feature to move transactions between invoices by long-pressing a transaction item.

### Steps:

1.  **Create `components/TransactionOptionsModal.tsx`**:
    *   This will be a new component using the `BottomModal` (same style as "Configurar Fatura").
    *   It will display options to:
        *   **Move to Previous Invoice** (changes date to before the current closing date).
        *   **Move to Next Invoice** (changes date to after the current closing date).
        *   **Change Date** (allows picking a specific date).
        *   **Refund** (if applicable).
        *   **Delete** (delete transaction).
    *   The styling will match the dark theme of the application.

2.  **Update `components/CreditCardInvoice.tsx`**:
    *   Add state to manage the visibility of `TransactionOptionsModal` and the currently selected transaction.
    *   Modify the `TransactionItem` component to accept an `onLongPress` callback that triggers this new modal instead of the inline delete buttons.
    *   Implement the `handleMoveTransaction` logic:
        *   Calculates the new date based on the user's selection (Next/Previous invoice relative to the current closing date).
        *   Updates the transaction in Firebase using `databaseService.saveOpenFinanceCreditCardTransaction` with the new date.
    *   Integrate the new `TransactionOptionsModal` into the render method.

3.  **Refine Transaction Logic**:
    *   Ensure that moving a transaction updates the list immediately by refreshing or updating the local state.
